# et2 — skills branch

This branch holds **agent skills only** — install into other et2 branches while working.

## Quick start

```bash
./scripts/install-skills.sh /path/to/your/worktree
```

## Contents

- `.cursor/skills/` — caveman, cavecrew, caveman-compress (+ Python scripts), handoff, tdd, grill-with-docs
- `.cursor/rules/caveman-ultra-default.mdc` — **caveman ultra default forever**
- `context/` — compressed et2 context docs (`.original.md` backups kept)
- `scripts/compress-context-docs.sh` — batch rules-only compression (no Claude)

See `.cursor/skills/README.md` for details.
